事件冒泡
如果父元素绑定了某种事件，通过触发子元素 导致父元素的事件触发，这就是事件冒泡
事件冒泡是自下而上的
阻止事件冒泡
在事件函数中写入e.stopPropagation(); 即可阻止事件的冒泡
或者写入return false亦可以阻止事件冒泡



事件捕获
由上而下 触发父元素的事件导致子元素的事件被触发 这就是事件捕获，
可以通过事件委托来解决这个问题 
即addEventlisenner来绑定事件 第三个参数若为true则是触发事件捕获
				若为false就是触发事件冒泡
e.stopPropagation()也可以阻止事件捕获